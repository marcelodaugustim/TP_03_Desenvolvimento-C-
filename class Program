using System;
using System.Collections.Generic;
using System.Diagnostics.Tracing;
using System.Linq;


namespace TP3_DesenvolvimentoC
{
    class Program
    {
        static void Main(string[] args)
        {

            List<Pessoa> listaAniversarios = new List<Pessoa>();

            Console.WriteLine("Agenda de Aniversário");
            
            while (true)
            {

                Console.WriteLine("Menu:");

                Console.WriteLine(" 1 - Inserir");
                Console.WriteLine(" 2 - Pesquisar");
                Console.WriteLine(" 3 - Sair ");

                Console.Write("Digite a opção desejada: ");
                string opcaoEscolhida = Console.ReadLine();

                Console.WriteLine("*************************");

                if (opcaoEscolhida == "1")
                {
                    var aniversariante = new Pessoa();
                    aniversariante.CriarPessoa();
                    listaAniversarios.Add(aniversariante);
                }
              
               foreach (Pessoa pessoas in listaAniversarios)
                {
                    Console.WriteLine(pessoas.Nome);
                }
            }
        }

    }
}
